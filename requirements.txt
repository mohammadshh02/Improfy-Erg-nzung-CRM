flask>=3.0
openpyxl>=3.1

# Für „Alte Unterlagen ablegen" (dokument_lesen.py). Ohne diese Pakete läuft das OS
# weiter, es liest dann aber nur Excel und reinen Text; PDF und Word werden abgelehnt.
pymupdf>=1.24        # PDF-Text in Lesereihenfolge
pypdf>=4.0           # Rückfall für PDF
python-docx>=1.1     # Word
odfpy>=1.4           # OpenOffice
pillow>=10.0         # Bewerbungsfoto auf Druckgröße bringen

# Für die Selbsttests. `bedienprobe.py` parst damit die gerenderten Seiten und
# drückt jeden Knopf so, wie ein Browser ihn abschickt. Ohne das Paket stirbt der
# Test, der vor jedem Push laufen soll, vor der ersten Prüfung.
lxml>=5.0            # geprüft gegen 6.1.2
